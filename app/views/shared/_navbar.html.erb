<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to "JourneyMemoir", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if Current.user %>
          <li class="nav-item">
            <%= link_to "My Entries", entries_path , class: "nav-link"%>
          </li>
          <li class="nav-item">
            <%= link_to "New Entry", new_entry_path , class: "nav-link"%>
          </li>
        <% else %>
          <!-- Add any other links for non-logged-in users -->
        <% end %>
      </ul>
      <ul class="navbar-nav">
        <% if Current.user %>
          <li class="nav-item">
            <div class="d-flex align-items-center">
              <span class="navbar-text me-2"><%= Current.user.email %></span>
            </div>
          </li>
          <li class="nav-item">
            <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-lg btn-secondary nav-link text-white" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", sign_in_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", sign_up_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
